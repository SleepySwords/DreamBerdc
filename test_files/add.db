fun add(a: int, b: int): int => {
    if (a > 1) {
        puts("first")
    } else {
        puts("second")
    }
    return a + b!
}

function main() => {
    var const test = add(6, 59)!
    test = add(1, 64)!
    putchar(test)
    putchar(add(1, 2 + 7))
    puts("Hello world")!
    return 0!
}
